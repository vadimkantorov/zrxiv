recent_docs: 50

data_source: data
defaults:
  -
    scope:
      path: "data/tags/*.md"
    values:
      permalink: /:basename/
      layout: default
  -
    scope:
      path: "data/../data/../data/tags/*.md"
    values:
      permalink: /:basename.bib/
      layout: bibtex   
  -
    scope:
      path: "data/../data/tags/*.md"
    values:
      permalink: /:basename.json/
      layout: json
      
plugins:
  - jekyll-optional-front-matter
  
#collections_dir: data
#collections:
#  tags:
#    output: true
#    permalink: /:collection/:path/

